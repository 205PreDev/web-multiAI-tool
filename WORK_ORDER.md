# 작업지시서

**지금 할 작업 하나만 담습니다.** 전체 목록은 `TODO.md`에 있으며 여기에 옮겨 적지 않습니다.
작업이 끝나면 `TODO.md`를 갱신한 뒤 다음 작업으로 이 파일을 새로 씁니다.

---

## 현재 작업 — 프로젝트 초기화

`TODO.md` 0단계 / 대응 항목은 초기화와 개발 환경 정리, 라이브러리 설치까지 세 건입니다.

### 목표

**Vite와 React 19, TypeScript로 프로젝트를 세우고 빈 3D 씬이 브라우저에 렌더되는 것까지 확인합니다.**

### 왜 지금인가

1단계의 에디터 골격이 이 프로젝트의 최대 위험 구간이고, 거기에 들어가려면 씬이 화면에 뜨는 상태가 먼저 필요합니다. 0단계는 그 발판을 만드는 작업이며 판단할 것이 거의 없으므로 빠르게 통과시킵니다.

### 선행 확인

작업을 시작하기 전에 프로젝트 폴더명이 `web-multiAI-tool`로 바뀌었는지 확인합니다. 아직 `web-multi-tool`이라면 사용자에게 알리고, 폴더명과 무관한 작업이므로 그대로 진행합니다.

### 하는 것

1. Vite로 React 19 + TypeScript 템플릿을 생성합니다. 프로젝트 루트에 이미 `docs`와 `.local`, `CLAUDE.md`가 있으므로 **기존 파일을 덮어쓰지 않도록** 주의합니다.
2. ESLint와 Prettier를 설정하고 경로 별칭을 잡습니다.
3. 폴더 구조를 잡습니다. 최소한 씬 상태와 커맨드, 뷰포트 UI, 외부 API 어댑터가 서로 섞이지 않게 나눕니다.
4. three.js와 `@react-three/fiber`, `drei`, `leva`, `zustand`를 설치합니다.
5. 빈 씬을 띄웁니다. 그리드와 궤도 카메라, 큐브 하나면 충분합니다.
6. `WebGPURenderer`를 적용하고 **WebGL2로 폴백되는 경로가 실제로 동작하는지** 확인합니다.
7. `CLAUDE.md`의 실행 절을 실제 명령으로 채웁니다. 현재 "미정"으로 비어 있습니다.

### 하지 않는 것

- 에디터 기능은 손대지 않습니다. 기즈모와 아웃라이너, 인스펙터, undo와 redo는 전부 1단계입니다.
- 서버는 세우지 않습니다. 2단계입니다.
- 외부 API는 연결하지 않습니다. 3단계입니다.

### 완료 판정

아래를 모두 만족하면 완료입니다.

- [ ] `npm run dev`로 개발 서버가 뜨고 브라우저에 빈 씬이 보입니다.
- [ ] 궤도 카메라로 회전과 줌이 됩니다.
- [ ] WebGPU를 지원하는 환경과 그렇지 않은 환경 모두에서 렌더됩니다. 폴백 여부를 콘솔이나 화면에 표시해 확인할 수 있습니다.
- [ ] `npm run build`가 오류 없이 끝납니다.
- [ ] `CLAUDE.md`의 실행 절에 실제 명령이 적혀 있습니다.

### 참조

- `docs/REQUIREMENTS.md` §8 기술 스택, N-1 렌더러
- `CLAUDE.md` 제약 전체

### 제약

- 렌더러는 **WebGL2 폴백을 반드시 유지합니다.** WebGPU 단독으로 가지 않습니다.
- API 키를 클라이언트에 두지 않습니다. 이 단계에서는 키를 쓸 일이 없으므로 `.env`에 미리 넣어두지도 않습니다.

---

## 다음 작업

1단계의 첫 항목인 three.js 공식 에디터 Command 패턴 이식입니다.
