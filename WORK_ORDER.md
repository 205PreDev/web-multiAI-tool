# 작업지시서

**지금 할 작업 하나만 담습니다.** 전체 목록은 `TODO.md`에 있으며 여기에 옮겨 적지 않습니다.
작업이 끝나면 `TODO.md`를 갱신한 뒤 다음 작업으로 이 파일을 새로 씁니다.

---

## 현재 작업 — 뷰포트와 아웃라이너

`TODO.md` 1단계의 A-1과 A-2입니다. 커맨드 스택 위에 **처음으로 사람이 쓰는 화면**을 올립니다.

### 목표

**씬 그래프가 화면에 보이고, 거기서 고른 것이 뷰포트와 이어집니다.**

### 왜 지금인가

커맨드는 섰지만 지금 그것을 부르는 것은 임시 패널의 버튼 네 개뿐입니다. **아웃라이너가 커맨드의 첫 실제 소비자**이고, 여기서 선택·이름 변경·삭제·계층 이동이 실제 사용자 조작으로 들어오면 커맨드 설계가 맞는지가 처음 시험받습니다.

기즈모(A-3)·인스펙터(A-4)보다 먼저인 이유는 **선택이 그 둘의 전제**이기 때문입니다. 지금 스토어에 `selectedIds` 와 `select` 가 있는데 **`select` 를 부르는 코드가 저장소에 하나도 없습니다.**

### 하는 것

1. **뷰포트 (A-1)** — 궤도 카메라 / 그리드 / 그림자. 0단계에서 선 것을 정리해 씁니다. `Grid` 는 WebGPU에서 안 되므로 `GridHelper` 그대로입니다
2. **아웃라이너 (A-2)** — 계층 표시 / 선택 / 이름 변경 / 삭제. **전부 커맨드를 거칩니다**
3. **선택을 배선합니다.** 아웃라이너 클릭 → `select` → 상태 바의 "선택 노드" 표시. 지금 하드코딩된 "선택 없음"을 없앱니다
4. **프리미티브·라이트 추가 (A-11)** — Add 커맨드가 이미 있으므로 UI만 붙입니다. `CommandProbe` 를 지우고 이것으로 대체합니다
5. **에디터 레이아웃 골격** — `docs/UX.md` 2절의 배치와 파이프라인 스테퍼 자리
6. **디자인 토큰 3층 골격과 `client/ui/` 규칙 (N-14)** — 첫 UI가 여기서 나오므로 리터럴 색이 들어오기 전에 세웁니다. 린트로 강제합니다

### 하지 않는 것

- 기즈모(A-3)·인스펙터(A-4)는 다음입니다. **다만 연속 조작 병합은 그 지시서에서 반드시 함께 다룹니다** — `CHANGELOG.md` 8차의 ⚠️ 참조
- 씬 직렬화·IndexedDB(A-6, H-1)는 UI가 선 뒤입니다
- 다중 선택(A-7)은 P1입니다. `selectedIds` 가 배열인 것은 자리만 파둔 것입니다

### 완료 판정

**1차 — 도구 검증**

- [ ] `npm run verify` 통과
- [ ] **아웃라이너의 모든 조작이 되돌려집니다** — 이름 변경·삭제·계층 이동을 섞어서 한 뒤 전부 undo 하면 처음 상태로 돌아옵니다
- [ ] `sceneAccess.test.ts` 가 계속 통과합니다 — 새 UI가 커맨드를 우회하지 않았다는 뜻입니다
- [ ] 리터럴 색이 없습니다 (N-14 린트)
- [ ] `/code-review` 지적 사항 처리

**2차 — 미지 에이전트 검증**

**실행합니다.** 발동 조건 3(요구사항 ID 항목을 `[x]` 로 바꿈)에 걸립니다. A-1·A-2·A-11·N-14가 대상입니다.

**3차 — 사용자 검증**

- [ ] 아웃라이너에서 고른 것이 뷰포트에서 어느 것인지 알 수 있습니다
- [ ] 계층을 끌어 옮기는 조작이 예상대로 동작합니다
- [ ] 레이아웃이 `docs/UX.md` 2절과 어긋나지 않습니다

### 참조

- `docs/REQUIREMENTS.md` A-1 / A-2 / A-11 / N-14
- `docs/UX.md` 2절(레이아웃) · 3.7절(드래그 앤 드롭은 전부 커맨드 경유) · 6절(디자인 시스템)
- `client/src/editor/viewport/README.md` — 새 drei 컴포넌트를 쓰기 전에 호환 확인
- `docs/DECISIONS.md` D-20 / D-21 / D-25

### 제약

- **커맨드가 아닌 경로로 씬을 바꾸지 않습니다.** 이제 이것은 `sceneAccess.test.ts` 가 검사합니다
- **선택 외곽선에 drei `Outlines` 를 쓸 수 없습니다** (D-21의 ⚠️). 대안을 정하고 `viewport/README.md` 표를 갱신합니다
- 3D 라벨에 drei `Text`(troika)도 막혀 있습니다. 아웃라이너는 DOM이므로 해당 없지만 뷰포트에 이름을 띄우려 할 때 걸립니다

---

## 다음 작업

기즈모(A-3)·인스펙터(A-4) — **연속 조작 병합을 함께** 넣습니다.
