# 작업지시서

**지금 할 작업 하나만 담습니다.** 전체 목록은 `TODO.md`에 있으며 여기에 옮겨 적지 않습니다.
작업이 끝나면 `TODO.md`를 갱신한 뒤 다음 작업으로 이 파일을 새로 씁니다.

---

## 현재 작업 — 커맨드 스택 이식

`TODO.md` 1단계의 첫 세 항목입니다. Command 패턴 이식, Zustand 씬 그래프 스토어, **커맨드 직렬화 설계**까지입니다.

### 목표

**three.js 공식 에디터의 Command 패턴을 가져와, 씬 조작이 되돌릴 수 있고 JSON으로 오갈 수 있는 상태로 만듭니다.**

### 왜 지금인가

이 프로젝트의 최우선 위험이 에디터 기본기이고(10절), 그 한가운데가 커맨드 스택입니다. 그리고 **이 구간의 설계가 나중에 세 곳을 결정합니다** — undo/redo(A-5), 조수(F-3), 협업 전파(K-4)가 전부 같은 커맨드 객체를 소비합니다. 뒤에서 고치면 세 곳이 함께 흔들리므로 여기서 한 번에 맞춥니다.

### 하는 것

1. **three.js 공식 에디터(MIT)의 `Command` 기반 구조를 이식합니다.** 직접 설계하지 않습니다. 출처와 라이선스 고지를 코드에 남깁니다.
2. **Zustand로 씬 그래프 스토어를 구성합니다.** 노드 추가·삭제·이름 변경·트랜스폼 변경이 최소 단위입니다.
3. **커맨드를 직렬화 가능하게 설계합니다.** 각 커맨드는 `{type, payload}` 로 JSON 왕복이 되고, 같은 씬 상태에 같은 커맨드를 적용하면 결과가 같아야 합니다.
4. **커맨드를 외부에서 호출할 수 있는 진입점을 하나 둡니다.** 7단계 조수가 이 경로만 쓰게 됩니다 — UI 이벤트 핸들러 안에 로직을 묻지 않습니다.
5. undo / redo 스택과 단축키(Ctrl+Z / Ctrl+Y)를 붙입니다.

### 하지 않는 것

- 아웃라이너·기즈모·인스펙터 UI는 이 지시서 다음입니다. **지금은 커맨드와 스토어까지**이며, 확인은 임시 버튼이나 콘솔로 합니다.
- 씬 직렬화와 IndexedDB 영속(A-6, H-1)은 커맨드가 선 뒤에 붙입니다.
- 서버 동기화(H-8)는 9단계입니다.

### 완료 판정

**1차 — 도구 검증**

- [ ] `npm run verify` 통과
- [ ] **커맨드 JSON 왕복 단언** — 직렬화 후 역직렬화한 커맨드를 같은 씬에 적용하면 결과가 원본과 같습니다. 이 단언이 이번 작업의 핵심 산출물입니다
- [ ] undo → redo → undo 를 반복해도 씬 상태가 어긋나지 않습니다
- [ ] `/code-review` 지적 사항 처리

**2차 — 미지 에이전트 검증**

**실행합니다.** 변경 경로가 `client/editor/commands/**` 에 걸리며(발동 조건 2), 이 경로는 1단계에 한해 단계 완료 시 한 번으로 묶는다는 단서가 붙어 있습니다 — **다만 커맨드 직렬화는 세 소비자가 걸린 계약이므로 이 지시서에서 한 번 돌립니다.** 넘기는 것은 변경된 코드와 `docs/REQUIREMENTS.md` A절·F절·K절, `CLAUDE.md`의 제약, 위 완료 판정뿐입니다.

**3차 — 사용자 검증**

- [ ] 조작을 되돌리고 다시 실행했을 때 화면이 기대대로 움직입니다

### 참조

- `docs/REQUIREMENTS.md` A-5 / F-3 / K-4, 8.3절 차용
- `docs/ARCHITECTURE.md` 1절 경계, 6절 협업 프로토콜의 커맨드 전파
- `CLAUDE.md` 제약 8

### 제약

- **커맨드가 아닌 경로로 씬을 바꾸지 않습니다.** 컴포넌트가 스토어를 직접 변경하기 시작하면 undo가 조용히 무너집니다.
- three.js 에디터에서 가져온 코드는 **어디서 가져왔는지 파일 상단에 적습니다.**

---

## 다음 작업

1단계의 뷰포트·아웃라이너·기즈모·인스펙터 UI입니다.
