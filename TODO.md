# TODO

전체 작업 목록입니다. **진행 상태는 이 파일에서만 관리하며 다른 곳에 사본을 두지 않습니다.**

- 상태 기호는 `[ ]` 미착수, `[~]` 진행 중, `[x]` 완료입니다.
- 항목 끝의 괄호는 `docs/REQUIREMENTS.md`의 요구사항 ID입니다.
- 아래 순서는 의존 관계와 위험도를 함께 고려해 정했습니다. **위험이 큰 것을 앞에 둡니다.**
- **0–8단계가 데모 A(P0), 9단계가 데모 B(P1·서비스화), 10–11단계가 확장(P2)입니다.** 우선순위 의미는 `docs/REQUIREMENTS.md` 4절 서두를 따릅니다.

---

## 현재 위치

| | |
| --- | --- |
| 진행 중인 단계 | 0단계 준비 |
| 다음 작업 | `WORK_ORDER.md` 참조 |
| 마지막 갱신 | 2026-08-10 |

---

## 0단계 — 준비

- [ ] 프로젝트 폴더명을 `web-multiAI-tool`로 변경 — 사용자가 세션 밖에서 수동 처리
- [ ] Vite + React 19 + TypeScript로 프로젝트를 초기화
- [ ] 개발 환경 정리 — ESLint / Prettier / 경로 별칭 / 폴더 구조
- [ ] three.js / R3F / drei / leva / Zustand를 설치하고 빈 씬이 렌더되는 것까지 확인
- [ ] WebGPURenderer를 적용하고 WebGL2 폴백이 실제로 동작하는지 확인 (N-1)

## 1단계 — 에디터 골격

**이 프로젝트의 최대 위험 구간입니다.** 화면에 드러나지 않으면서 기간을 크게 소모하므로 three.js 공식 에디터의 Command 패턴을 차용해 단축합니다. (8.1절, 10절)

- [ ] three.js 공식 에디터의 Command 패턴을 이식
- [ ] Zustand 기반 씬 그래프 스토어를 구성
- [ ] **커맨드를 직렬화 가능하고 결정적으로 재생되게 설계** — undo(A-5) / 7단계 조수(F-3) / 10단계 협업 전파(K-4)의 세 소비자가 같은 커맨드를 씁니다. 완료 판정에 "커맨드 JSON 왕복 후 재생 결과 동일" 단언 포함 (8.3절)
- [ ] undo / redo를 동작시킴 (A-5)
- [ ] 씬 직렬화와 복원 — 저장 / 불러오기 / 새로고침 후 복원 (A-6)
- [ ] IndexedDB 영속 — **서버 없이도 저장과 새로고침 복원이 동작할 것** (H-1)
- [ ] 뷰포트 — 궤도 카메라 / 그리드 / 그림자 (A-1)
- [ ] 아웃라이너 — 계층 표시 / 선택 / 이름 변경 / 삭제 (A-2)
- [ ] 기즈모 — 이동 / 회전 / 스케일, 로컬과 월드 전환 (A-3)
- [ ] 인스펙터 — 트랜스폼 / 머티리얼 / 메타데이터 편집 (A-4)
- [ ] 프리미티브·라이트 추가 — 공식 에디터의 Add 커맨드가 이식에 포함되므로 이 단계에서 함께 활성화 (A-11)
- [ ] 디자인 토큰 3층 골격과 `client/ui/` 공유 컴포넌트 규칙 수립 — 첫 UI부터 리터럴 색 금지, 린트로 강제 (N-14)
- [ ] 에디터 레이아웃 골격 — `docs/UX.md` 2절의 배치와 파이프라인 스테퍼 자리

## 2단계 — 서버 기반

서버 역할은 `docs/ARCHITECTURE.md`가 정본입니다. **인증은 9단계이지만, 모든 테이블과 계약에 `user_id` 자리를 지금 파둡니다** — 개발 계정 하나로 채우고 9단계에서 스키마 변경 없이 전환합니다 (ARCHITECTURE 4절).

- [ ] Spring Boot 스캐폴딩과 HTTP 계약 확정 — `docs/ARCHITECTURE.md` 5절의 계약에 대고 코딩 (8.2절)
- [ ] PostgreSQL 스키마 — users(개발 계정 1행) / projects / generation_jobs / credit_ledger / scene_revisions / assets / asset_versions (ARCHITECTURE 4절)
- [ ] 외부 API 중계 엔드포인트 — 키는 서버에만, 클라이언트는 제공자를 직접 부르지 않음 (H-4, N-7)
- [ ] S3 호환 스토리지(로컬 MinIO) + 서명 URL — 제공자 URL 만료 전에 내려받아 저장 (H-2)
- [ ] 생성 작업 상태 머신과 조회 — 영속 정체성은 제공자가 준 작업 ID (H-3, N-4, ARCHITECTURE 3.1)
- [ ] STOMP 진행률 푸시 — 정본은 REST 조회, 푸시는 최적화 (B-4)
- [ ] 후처리 폴백 엔드포인트 — `shared/postprocess/`를 Node 자식 프로세스로 실행 (H-5, C-2)
- [ ] 크레딧 원장 골격 — 선차감·정산·환원 흐름, 개발 계정 기준 (I-2의 기반, ARCHITECTURE 3.2)
- [ ] 지출 전역 캡 — 80%에서 확인, 100%에서 생성 엔드포인트 거부 (I-4, N-6)

## 3단계 — AI 생성

- [ ] 생성 백엔드 어댑터와 **모델 레지스트리**를 정의 — 항목마다 코드·가중치 라이선스를 별도 필드로 기록하고 레지스트리 우회 호출을 CI가 막게 함 (N-8, B-5의 근거)
- [ ] Tripo 텍스트에서 메시 생성 (B-1)
- [ ] Tripo 이미지에서 메시 생성 (B-2)
- [ ] 텍스처 생성과 재생성 (B-3)
- [ ] 실패 / 타임아웃 / 쿼터 초과를 구분해 표시하고 재시도 제공 — 오류 문구는 `docs/UX.md` 5절 사전 (N-5)
- [ ] 호출별 예상 크레딧을 화면에 표시 (I-3의 기반, N-6)
- [ ] 생성 이력 기록 — 프롬프트·파라미터·비용을 asset_versions.source에 (B-6)
- [ ] **데모 레일** — 생성 결과 GLB를 픽스처로 커밋하고 `npm run demo:rail` 통과

## 4단계 — 후처리

**순서를 바꾸지 않습니다.** 정리에서 UV, 리깅 순으로 진행하며, 토폴로지가 정돈되지 않은 상태로 리깅에 들어가면 리깅 품질까지 무너집니다.

- [ ] glTF-Transform으로 지오메트리 정리 (C-1)
- [ ] xatlas UV 언랩을 웹워커에서 실행 (C-2)
- [ ] UV 서버 폴백 경로 (C-2)
- [ ] Tripo 오토리깅과 스켈레톤 시각화, 성공 판정 네 가지 (C-3)
- [ ] 파이프라인 스테퍼 UI — 게이트 수치 노출, 앞 단계 미통과 시 비활성 (UX 2.1절)
- [ ] 파이프라인 입구를 씬 안의 임의 메시로 — 수동·임포트 메시의 정리→UV 통과 확인. 리깅·텍스처의 외부 메시 지원은 Tripo 제약 확인(ARCHITECTURE 13절 7번) 후 판정 (4절 서두)
- [ ] **순서 고정 근거 1회 실험** — 정리·UV를 거친 리깅과 곧바로 한 리깅을 비교해 표로 남김
- [ ] C-2 임계값 실측 — 삼각형 15만과 45초를 실제 값으로 조정
- [ ] **데모 레일** — 정리본·리깅본 픽스처 추가, 15만 삼각형 폴백 픽스처 포함

## 5단계 — 애니메이션

- [ ] 타임라인 — 재생 / 정지 / 스크럽 / 루프 (D-1)
- [ ] 클립 관리와 전환 (D-2)
- [ ] 이벤트 마커 — 특정 프레임에 이름을 가진 이벤트를 꽂음 (D-3)
- [ ] 모션 적용 — Tripo 리타깃의 고정 프리셋 중 선택 (D-4)
- [ ] **데모 레일** — `slash` 클립과 `hit` 이벤트를 담은 픽스처 추가

## 6단계 — 오디오

- [ ] ElevenLabs로 효과음 생성 — **지속시간을 명시해 호출** (E-1)
- [ ] 오디오 라이브러리와 파형 표시 (E-2)
- [ ] 클립·이벤트 이름에서 효과음 프롬프트를 제안 — 규칙 기반, 교체 가능한 인터페이스 뒤에 배치 (E-5)
- [ ] 이벤트 마커에 효과음 바인딩 (E-3)
- [ ] `AudioContext.currentTime` 기반 룩어헤드 스케줄러 + **예약 이력 훅** (E-4)
- [ ] 타이밍 자동 측정과 T1–T5 단언 — `docs/WMT_SCHEMA.md` 6.1절
- [ ] **데모 레일** — 효과음 픽스처 추가

## 7단계 — 자연어 조수

**툴은 두세 개로 한정합니다.** 조수는 새 조작 경로를 만들지 않고 1단계에서 만든 커맨드만 호출합니다.

- [ ] 툴 스키마를 정의 — `strict: true`, `additionalProperties: false`, 호출 시점 명시 (F-2)
- [ ] 씬 요약 JSON과 개별 상세 질의를 분리 (F-2)
- [ ] 채팅으로 씬 조작 — 트랜스폼 변경, 머티리얼 변경 (F-1)
- [ ] 각 툴을 기존 커맨드에 1:1로 연결해 undo가 자동으로 따라오게 함 (F-3)
- [ ] **데모 레일** 통과 확인

## 8단계 — 익스포트

**데모 A의 완료 조건이 걸린 단계입니다.** 목적문의 "게임 엔진에 바로 넣을 수 있는"을 실제로 검증합니다.

- [ ] glTF / GLB 내보내기 (G-1)
- [ ] `extras`의 `wmt` 스키마로 이벤트와 오디오 바인딩 직렬화 (G-2)
- [ ] 오디오 파일 동봉 — 외부 참조 또는 zip (G-3)
- [ ] Unity 임포터 스크립트 — `AnimationEvent` + `AudioSource` 복원 (G-4)
- [ ] 단위와 축 변환, 옵션 노출 (G-5)
- [ ] **데모 레일** — 최종본 픽스처 추가, 스키마 왕복 단언 7종 통과
- [ ] **대표 데모 A 통합 시연** — 픽스처가 아니라 외부 API를 실제로 호출해, 텍스트 입력부터 Unity 재생까지 한 세션에서 끊김 없이 (1.4절, 11절)

---

## 9단계 — 서비스화 (P1, 데모 B)

**공개 배포가 이 단계의 작업입니다.** 착수 전에 `docs/ARCHITECTURE.md` 13절의 미확인 목록(생성물 권리, 스토리지·호스팅 요금, OAuth 검수 요건)을 사용자가 확인합니다.

- [ ] OAuth 로그인(Google / GitHub)과 세션 쿠키 — 개발 계정에서 실계정으로 전환 (I-1)
- [ ] 사용자별 크레딧 — 가입 지급 200, 잔액 상시 표시, 부족 시 호출 차단 (I-2, I-3)
- [ ] 남용 방어 — 동시성 1 / 시간당 상한 / IP당 가입 제한 / 업로드 검증 (I-5, N-9)
- [ ] 다중 프로젝트 대시보드 (I-7)
- [ ] 씬 서버 동기화 — 리비전 업로드, baseSeq 충돌 규칙, 동기화 상태 표시 (H-8, ARCHITECTURE 7절)
- [ ] 리비전 이력에서 복원 (H-9)
- [ ] 에셋 버전 이력과 계보 화면 (J-4, B-6)
- [ ] 공유 링크 발행·회수 (J-2)
- [ ] **공유 뷰어 — 경량 번들, 모바일 동작, wmt 이벤트·오디오 재생** (J-3)
- [ ] 공개 갤러리와 신고 접수 (J-1)
- [ ] 생성물 이용 조건 문구 반영 — 7.2절 확인 결과 (미확인 해소 후)
- [ ] 관측성 — 요청 ID, 잡 감사 로그, 헬스체크 (N-12)
- [ ] 배포 — FE 정적 호스팅 + BE 컨테이너 + 관리형 PostgreSQL + 오브젝트 스토리지, DB 백업 확인 (N-10, ARCHITECTURE 9절)
- [ ] 도구 품질 P1 잔여 — 다중 선택(A-7) / 뷰포트 통계(A-8) / 드래그 앤 드롭 전체(A-12, UX 3.7절) / TRELLIS A/B(B-5) / LOD(C-4) / 압축(C-5) / 단계별 미리보기(C-6) / 스크린샷 맥락(F-4) / 파이프라인 자연어 지시(F-5) / glTF 임포트(G-6)
- [ ] 컴포넌트 카탈로그 완비 — 상태별(빈/로딩/오류, 라이트/다크) 격리 확인 (N-14)
- [ ] **대표 데모 B 통합 시연** — 실 배포 환경에서 가입부터 공유 링크 재생까지 (1.5절, 11절)

## 10단계 — 협업 (P2)

- [ ] 프로젝트 초대와 역할 (K-1)
- [ ] 프레즌스 표시 (K-2)
- [ ] 노드 lock — TTL 리스, 하트비트, 자동 해제 (K-3)
- [ ] 커맨드 브로드캐스트 동기화 (K-4)
- [ ] lock 요청과 이양 (K-5)

## 11단계 — 검수자와 심화 (P2)

- [ ] VLM pairwise 비교 (L-1)
- [ ] A/B·재생성 화면에 판정 배치, 이력 기록 (L-2, L-3)
- [ ] PBR 머티리얼 에디터 (A-9)
- [ ] 뷰포트 품질 옵션 (A-10)
- [ ] 프롬프트 리믹스 (B-7)
- [ ] 키프레임 편집 (D-5)
- [ ] 효과음 프롬프트 제안기를 LLM 호출로 교체 (E-5 확장)
- [ ] BYOK (I-6)
- [ ] OG·임베드 (J-5) / 갤러리 검색·태그 (J-6)
- [ ] 3D 공간 오디오 (E-6)

---

완료한 작업의 내용과 그때 발견한 것은 `CHANGELOG.md`에 남깁니다. **이 파일은 남은 것만 다룹니다.**
