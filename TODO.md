# TODO

전체 작업 목록입니다. **진행 상태는 이 파일에서만 관리하며 다른 곳에 사본을 두지 않습니다.**

- 상태 기호는 `[ ]` 미착수, `[~]` 진행 중, `[x]` 완료입니다.
- 항목 끝의 괄호는 `docs/REQUIREMENTS.md`의 요구사항 ID입니다.
- 아래 순서는 의존 관계와 위험도를 함께 고려해 정했습니다. **위험이 큰 것을 앞에 둡니다.**

---

## 현재 위치

| | |
| --- | --- |
| 진행 중인 단계 | 0단계 준비 |
| 다음 작업 | `WORK_ORDER.md` 참조 |
| 마지막 갱신 | 2026-08-05 |

---

## 0단계 — 준비

- [ ] 프로젝트 폴더명을 `web-multiAI-tool`로 변경 — 사용자가 세션 밖에서 수동 처리
- [ ] Vite + React 19 + TypeScript로 프로젝트를 초기화
- [ ] 개발 환경 정리 — ESLint / Prettier / 경로 별칭 / 폴더 구조
- [ ] three.js / R3F / drei / leva / Zustand를 설치하고 빈 씬이 렌더되는 것까지 확인
- [ ] WebGPURenderer를 적용하고 WebGL2 폴백이 실제로 동작하는지 확인 (N-1)

## 1단계 — 에디터 골격

**이 프로젝트의 최대 위험 구간입니다.** 화면에 드러나지 않으면서 기간을 크게 소모하므로 three.js 공식 에디터의 Command 패턴을 차용해 단축합니다. (8.1절, 10절)

- [ ] three.js 공식 에디터의 Command 패턴을 이식
- [ ] Zustand 기반 씬 그래프 스토어를 구성
- [ ] **커맨드를 외부에서 호출 가능한 형태로 설계** — 7단계 조수가 이 경로만 쓰게 됩니다
- [ ] undo / redo를 동작시킴 (A-5)
- [ ] 씬 직렬화와 복원 — 저장 / 불러오기 / 새로고침 후 복원 (A-6)
- [ ] IndexedDB 영속 — **서버 없이도 저장과 새로고침 복원이 동작할 것** (H-1)
- [ ] 뷰포트 — 궤도 카메라 / 그리드 / 그림자 (A-1)
- [ ] 아웃라이너 — 계층 표시 / 선택 / 이름 변경 / 삭제 (A-2)
- [ ] 기즈모 — 이동 / 회전 / 스케일, 로컬과 월드 전환 (A-3)
- [ ] 인스펙터 — 트랜스폼 / 머티리얼 / 메타데이터 편집 (A-4)

## 2단계 — 서버 기반

**서버는 세 가지만 합니다** — 키를 클라이언트에서 떼어놓기, 수 분짜리 작업의 상태 들고 있기, 후처리 폴백 실행하기. 인증과 다중 프로젝트는 P1입니다.

- [ ] Spring Boot 스캐폴딩과 HTTP 계약 확정 — 클라이언트는 프레임워크가 아니라 계약에 대고 코딩합니다 (8.2절)
- [ ] 외부 API 중계 엔드포인트 — 키는 서버에만, 클라이언트는 제공자를 직접 부르지 않음 (H-4, N-7)
- [ ] 산출물 바이너리 스토리지 — 제공자 URL 만료 전에 내려받아 저장 (H-2)
- [ ] 생성 작업 상태 보관과 조회 — 영속 정체성은 제공자가 준 작업 ID (H-3, N-4)
- [ ] STOMP 진행률 푸시 (B-4)
- [ ] 후처리 폴백 엔드포인트 — `shared/postprocess/`를 Node 자식 프로세스로 실행 (H-5, C-2)
- [ ] 지출 상한 — 80%에서 확인, 100%에서 생성 엔드포인트 거부 (N-6)

## 3단계 — AI 생성

- [ ] 생성 백엔드 어댑터와 **모델 레지스트리**를 정의 — 항목마다 코드·가중치 라이선스를 별도 필드로 기록하고 레지스트리 우회 호출을 CI가 막게 함 (N-8, B-5의 근거)
- [ ] Tripo 텍스트에서 메시 생성 (B-1)
- [ ] Tripo 이미지에서 메시 생성 (B-2)
- [ ] 텍스처 생성과 재생성 (B-3)
- [ ] 실패 / 타임아웃 / 쿼터 초과를 구분해 표시하고 재시도 제공 (N-5)
- [ ] 호출별 예상 비용을 화면에 표시 (N-6)
- [ ] **데모 레일** — 생성 결과 GLB를 픽스처로 커밋하고 `npm run demo:rail` 통과

## 4단계 — 후처리

**순서를 바꾸지 않습니다.** 정리에서 UV, 리깅 순으로 진행하며, 토폴로지가 정돈되지 않은 상태로 리깅에 들어가면 리깅 품질까지 무너집니다.

- [ ] glTF-Transform으로 지오메트리 정리 (C-1)
- [ ] xatlas UV 언랩을 웹워커에서 실행 (C-2)
- [ ] UV 서버 폴백 경로 (C-2)
- [ ] Tripo 오토리깅과 스켈레톤 시각화, 성공 판정 네 가지 (C-3)
- [ ] **순서 고정 근거 1회 실험** — 정리·UV를 거친 리깅과 곧바로 한 리깅을 비교해 표로 남김
- [ ] C-2 임계값 실측 — 삼각형 15만과 45초를 실제 값으로 조정
- [ ] **데모 레일** — 정리본·리깅본 픽스처 추가, 15만 삼각형 폴백 픽스처 포함

## 5단계 — 애니메이션

- [ ] 타임라인 — 재생 / 정지 / 스크럽 / 루프 (D-1)
- [ ] 클립 관리와 전환 (D-2)
- [ ] 이벤트 마커 — 특정 프레임에 이름을 가진 이벤트를 꽂음 (D-3)
- [ ] 모션 적용 — Tripo 리타깃의 고정 프리셋 중 선택 (D-4)
- [ ] **데모 레일** — `slash` 클립과 `hit` 이벤트를 담은 픽스처 추가

## 6단계 — 오디오

- [ ] ElevenLabs로 효과음 생성 — **지속시간을 명시해 호출** (E-1)
- [ ] 오디오 라이브러리와 파형 표시 (E-2)
- [ ] 클립·이벤트 이름에서 효과음 프롬프트를 제안 — 규칙 기반, 교체 가능한 인터페이스 뒤에 배치 (E-5)
- [ ] 이벤트 마커에 효과음 바인딩 (E-3)
- [ ] `AudioContext.currentTime` 기반 룩어헤드 스케줄러 + **예약 이력 훅** (E-4)
- [ ] 타이밍 자동 측정과 T1–T5 단언 — `docs/WMT_SCHEMA.md` 6.1절
- [ ] **데모 레일** — 효과음 픽스처 추가

## 7단계 — 자연어 조수

**툴은 두세 개로 한정합니다.** 조수는 새 조작 경로를 만들지 않고 1단계에서 만든 커맨드만 호출합니다.

- [ ] 툴 스키마를 정의 — `strict: true`, `additionalProperties: false`, 호출 시점 명시 (F-2)
- [ ] 씬 요약 JSON과 개별 상세 질의를 분리 (F-2)
- [ ] 채팅으로 씬 조작 — 트랜스폼 변경, 머티리얼 변경 (F-1)
- [ ] 각 툴을 기존 커맨드에 1:1로 연결해 undo가 자동으로 따라오게 함 (F-3)
- [ ] **데모 레일** 통과 확인

## 8단계 — 익스포트

**완료 조건이 걸린 단계입니다.** 목적문의 "게임 엔진에 바로 넣을 수 있는"을 실제로 검증합니다.

- [ ] glTF / GLB 내보내기 (G-1)
- [ ] `extras`의 `wmt` 스키마로 이벤트와 오디오 바인딩 직렬화 (G-2)
- [ ] 오디오 파일 동봉 — 외부 참조 또는 zip (G-3)
- [ ] Unity 임포터 스크립트 — `AnimationEvent` + `AudioSource` 복원 (G-4)
- [ ] 단위와 축 변환, 옵션 노출 (G-5)
- [ ] **데모 레일** — 최종본 픽스처 추가, 스키마 왕복 단언 7종 통과
- [ ] **대표 데모 통합 시연** — 픽스처가 아니라 외부 API를 실제로 호출해, 텍스트 입력부터 Unity 재생까지 한 세션에서 끊김 없이 (1.4절, 11절)

---

## P1 — 도구로 성립하려면 필요

- [ ] 다중 선택과 일괄 트랜스폼 (A-7)
- [ ] 뷰포트 통계 표시 (A-8)
- [ ] fal.ai TRELLIS 대체 백엔드와 A/B 비교 (B-5)
- [ ] 생성 이력 기록 (B-6)
- [ ] LOD 생성과 `MSFT_lod` 기록 (C-4)
- [ ] 메시 압축과 전후 용량 비교 (C-5)
- [ ] 후처리 단계별 전후 미리보기 (C-6)
- [ ] 효과음 프롬프트 제안기를 LLM 호출로 교체 (E-5 확장)
- [ ] 뷰포트 스크린샷을 조수 맥락으로 첨부 (F-4)
- [ ] 파이프라인을 자연어로 지시 (F-5)
- [ ] glTF / GLB 임포트 (G-6)
- [ ] 사용자 인증과 계정 분리 (H-6)
- [ ] 다중 프로젝트 관리 (H-7)

## P2 — 여력이 남을 때

- [ ] 키프레임 직접 편집 (D-5)
- [ ] 3D 공간 오디오 (E-6)

---

## 완료 기록

아직 없습니다.
