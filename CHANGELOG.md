# Changelog

작업 세션 단위로 무엇을 바꿨는지 기록합니다.

- **무엇을 했는지보다 왜 그렇게 했는지, 그리고 하다가 무엇을 발견했는지**를 남깁니다.
- 고치다 발견한 별개 문제는 그 자리에 적어둡니다. 다음 회차가 거기서 이어집니다.
- 남은 작업 목록은 `TODO.md`, 결정의 근거는 `docs/DECISIONS.md`에 있습니다. 여기에 옮겨 적지 않습니다.
- 기호는 ✏️ 수정, ➕ 추가, 🗑️ 삭제, ⚠️ 발견한 별개 이슈입니다.

---

## 2026-08-10 (7차 — 0단계 스캐폴딩)

설계를 마치고 첫 코드를 올렸습니다. Vite + React 19 + TypeScript에 three.js/R3F를 얹고 빈 씬이 브라우저에 뜨는 것까지입니다.

- ➕ npm 워크스페이스 루트 + `client/` — React 19.2 / Vite 8 / TS 6 / three 0.185 / R3F 9.7
- ➕ 폴더 구조 — `client/{editor,site,ui,api,export}` · `shared/{postprocess,types}` · `server/` · `unity/` · `fixtures/demo/`
- ➕ 렌더러 — WebGPU 우선, WebGL2 폴백. **어느 백엔드가 실제로 선택됐는지 상태 바에 표시**
- ➕ 디자인 토큰 3층 골격(N-14) — 첫 UI부터 리터럴 색 없이 갔습니다
- ✏️ `CLAUDE.md` 실행 절 — "미정"을 실제 명령으로. `npm run verify` 한 줄이 1차 도구 검증
- ✏️ `docs/ARCHITECTURE.md` 1절 — 공개 화면 폴더를 `public/`에서 `site/`로

**지시서와 어긋난 것 두 가지를 남깁니다.**

⚠️ **린터가 ESLint가 아니라 oxlint입니다.** 현재 Vite react-ts 템플릿의 기본값이 바뀌었습니다. 둘을 함께 두면 규칙이 갈라지고 느려지기만 하므로 템플릿 기본값을 따랐습니다. **N-14의 "리터럴 색 금지"는 CSS 규칙이라 애초에 ESLint가 아니라 stylelint의 일**이고, 그것은 토큰이 자리를 잡는 1단계에 붙입니다.

⚠️ **공개 화면 폴더가 `public/`이 될 수 없었습니다.** Vite가 `client/public/`을 정적 에셋 폴더로 예약합니다. 같은 이름이 두 뜻으로 존재하면 읽는 사람이 매번 헷갈리므로 `client/src/site/`로 옮기고 `docs/ARCHITECTURE.md`를 고쳤습니다.

**설계 판단 하나** — 폴백을 조용히 두지 않았습니다. three.js는 WebGPU가 없으면 알아서 WebGL2로 내려가는데, 그대로 두면 **나중에 "왜 느리지"의 원인을 짚을 수 없습니다.** 그래서 init 이후 실제 백엔드를 읽어 상태 바에 표시하고, 검증용 URL 질의를 두 개 두었습니다 — `?renderer=webgl2`(백엔드 직접 지정)와 `?renderer=nogpu`(`navigator.gpu`를 감춰 **폴백 경로 자체를 태움**). N-1이 요구하는 "폴백이 실제로 실행되는 환경"의 확인은 뒤쪽입니다. 앞의 것으로 대신하면 다른 코드 경로를 검증한 셈이 됩니다.

**1차 검증에서 코드 리뷰가 잡아낸 것** — 여섯 건을 고쳤습니다. 스캐폴딩이라 볼 것이 없을 줄 알았는데 **실제로 화면이 깨지는 버그가 하나 있었습니다.**

- 🐛 **뷰포트가 150px 띠로 렌더됐습니다.** `.viewport` 에 `flex: 1` 이 없어 R3F 컨테이너의 `height:100%` 가 auto 로 풀리고 canvas 고유 크기(300×150)로 주저앉았습니다. `min-height: 0` 만 적어두고 정작 늘어나라는 말을 빠뜨린 것입니다
- 🐛 **`?renderer=nogpu` 가 StrictMode에서 오작동할 수 있었습니다.** `navigator.gpu` 라는 전역을 건드리는데 R3F 마운트가 두 번 돌면 한쪽의 복원이 다른 쪽의 감추기를 풀어, 폴백을 태우려던 실행이 WebGPU를 받습니다. 생성을 직렬화했습니다
- 🐛 **`'gpu' in navigator` 가 감춘 뒤에도 참이었습니다.** `undefined` 를 소유 속성으로 덮는 방식이라 `in` 이 속습니다. `!== undefined` 로 바꿨습니다
- ✏️ 렌더러 생성이 완전히 실패하면 상태 바가 "준비 중"에 멈춘 채 아무 말도 하지 않았습니다. 실패도 보고 상태로 만들었습니다 — **무엇이 도는지 답하려고 만든 모듈이 정작 최악의 경우에 침묵하고 있었습니다**
- ✏️ R3F가 넘기는 기본값(`powerPreference` 등)을 버리고 객체를 새로 짓고 있었습니다
- ✏️ 그림자 받이 평면과 그리드가 같은 y=0 이라 z-파이팅했습니다
- ✏️ `App.tsx` 의 `data-theme="dark"` 가 `<html>` 의 값을 가려 라이트 테마가 앱 안쪽에 닿지 못했습니다

**3차 사용자 검증에서 나온 것 — 그리드가 아예 없었습니다.**

사용자가 "방향을 알 수 없다"며 콘솔의 경고 하나를 가져왔습니다. `THREE.NodeBuilder: Material "ShaderMaterial" is not compatible.`

🐛 **drei 의 `Grid` 는 WebGPU에서 동작하지 않습니다.** `WebGPURenderer` 는 NodeMaterial 위에 서 있어 raw `ShaderMaterial` 을 컴파일하지 못하는데, `Grid` 가 그 위에 서 있습니다. **오류가 아니라 경고이고 해당 오브젝트만 조용히 사라지므로**, 화면에는 그리드가 없고 콘솔에는 범인 이름도 없습니다. three 코어의 `GridHelper` 로 바꿨습니다.

⚠️ **이것은 이 컴포넌트 하나의 문제가 아니라 WebGPU를 고른 대가입니다.** drei 는 우리 스택에 있고 여러 헬퍼가 같은 방식으로 만들어져 있습니다. 그래서 세 가지를 함께 뒀습니다.

- **`warnIncompatibleMaterials.ts`** — 개발 빌드에서 씬을 훑어 **범인의 이름과 경로를 찍습니다.** three 의 경고가 말해주지 않는 것이 정확히 그것입니다
- **`client/src/editor/viewport/README.md`** — drei 컴포넌트별 호환 여부 표. 새로 도입할 때 먼저 확인하고 채웁니다
- 확인 방법도 적어뒀습니다 — `node_modules/@react-three/drei/core/<이름>.js` 에서 `shaderMaterial` 을 찾으면 됩니다

**WebGL2 폴백에서만 동작하는 화면을 만들면 N-1의 의미가 사라집니다.** 폴백은 대비책이지 기본 경로가 아닙니다.

- ➕ 화면 우측 하단 방향 기즈모 — 사용자 요청. 축을 누르면 그 방향으로 카메라가 돕니다

🐛 **그런데 그 기즈모가 앱 전체를 죽였습니다.** `GizmoViewport` 가 `gl.capabilities.getMaxAnisotropy()` 를 부르는데 **`capabilities` 는 `WebGLRenderer` 전용이라 WebGPU 에서는 `undefined`** 입니다. 경고로 끝나는 머티리얼 문제와 달리 이건 `Cannot read properties of undefined` 로 즉사하며, 그래서 기본 모드와 `nogpu` 모드 둘 다 아무것도 뜨지 않았습니다.

⚠️ **제 앞선 점검이 부족했습니다.** 머티리얼만 확인하고 "호환"으로 표에 적었는데, **확인해야 할 표면은 머티리얼과 렌더러 API 둘이었습니다.** 호환 표에 확인 명령을 두 줄로 늘리고 없는 API(`capabilities`·`extensions`·`outputEncoding`)를 명시했습니다.

- ➕ `AxisGizmo.tsx` — `GizmoViewport` 를 대신하는 자체 구현. **원본과 다른 곳은 `map-anisotropy` 를 넘기지 않는 한 줄뿐**이고 배치와 크기는 그대로 따랐습니다. `GizmoHelper` 자체는 Hud 와 카메라 동기화만 하고 렌더러 API 를 건드리지 않아 그대로 씁니다
- ➕ `ViewportErrorBoundary` — **3D 도구에서 검은 화면은 로딩 중과 구분되지 않습니다.** 이번에도 화면에는 아무것도 없고 콘솔을 열어야만 원인을 알 수 있었습니다. 렌더 단계의 예외를 잡아 무엇이 깨졌는지 화면에서 말하고 호환 표로 안내합니다

⚠️ **이 부류를 1차 도구 검증이 잡지 못합니다.** 타입·린트·빌드가 전부 통과한 채로 브라우저에서 죽었고, 같은 일이 두 번 연속 3차에서 드러났습니다. 브라우저 자동화가 저장소에 없기 때문이며, **N-2의 95백분위 프레임 측정도 결국 같은 수단을 요구**하므로 1단계에서 헤드리스 브라우저 검사를 붙이는 것을 검토합니다.
- ✏️ 그림자 절두체를 씬 크기에 맞춰 좁히고 맵을 2048로, `normalBias` 를 넣었습니다. 기본값(±5, 512)이 이 씬에는 굵어 가장자리가 지저분했습니다

⚠️ **"큐브가 세로로 갈라져 보인다"는 아직 원인을 확정하지 못했습니다.** 그리드가 없어 공간 기준이 사라진 상태에서 큐브의 모서리가 분할선처럼 읽힌 것일 수 있고, 그림자 절두체 경계일 수도 있습니다. **그리드와 기즈모를 복구한 상태에서 다시 확인해야 판정할 수 있습니다.**

⚠️ **`npm run verify` 에 테스트 단계가 없습니다.** 0단계에는 검사할 로직이 없어 넘어가지만, 1단계의 커맨드 JSON 왕복 단언이 첫 테스트가 되므로 그때 러너를 붙입니다. 라이선스 CI(7.1절)도 아직 없으며 어댑터가 생기는 3단계가 제자리입니다.

⚠️ **번들이 1.66MB(gzip 460KB)입니다.** three.js WebGPU 빌드와 drei가 대부분이며 에디터로서는 정상 범위지만, **J-3 공유 뷰어는 이 번들을 내려받으면 안 됩니다.** 9단계에서 번들 분리가 실제로 갈라지는지 수치로 확인해야 합니다.

⚠️ **프로젝트 폴더명이 아직 `web-multi-tool`입니다.** `web-multiAI-tool`로의 변경은 세션 밖 수동 작업으로 남아 있습니다.

---

## 2026-08-10 (6차 — ElevenAPI 탭 확인, ElevenLabs 항목 종결)

사용자가 ElevenAPI 탭을 확인해 넘겨줬습니다. **API는 별도 요금제가 아니라 Creative 구독의 크레딧을 공용**하거나 종량제를 고르는 구조이고, 계산기가 API 사용 기준으로 Creator($11/월)를 추천합니다. 탭의 단가 카드는 TTS·STT 모델(우리 미사용)이며 효과음은 구독 크레딧 차감 그대로입니다.

- ✏️ `docs/REQUIREMENTS.md` 7.2 — 확인 ④ 추가. **운영 계정 조건을 Starter 이상 구독(권장 Creator)으로 확정.** Creator의 121k크레딧 ≈ 효과음 3,000초
- ✏️ `docs/REQUIREMENTS.md` 9절 — ElevenLabs 행에 운영 계정 조건 반영, 미확인 표기 제거
- ✏️ `docs/ARCHITECTURE.md` 13절 2번 — **확인 완료.** 갤러리 재배포 문구 세부만 M9의 약관 재대조에 묶음

이로써 13절 미확인 목록에서 제공자 권리·단가 계열(1·2·3번 일부)이 정리됐고, 남은 것은 Tripo 승인 발송(실행 항목), fal.ai TRELLIS 단가, 호스팅·스토리지 요금, OAuth 검수 요건입니다.

---

## 2026-08-10 (5차 — ElevenLabs 요금제 확인 반영)

사용자가 ElevenLabs 요금 페이지(ElevenCreative 탭)를 확인해 넘겨줬습니다. "API가 없다"는 첫인상은 탭 구조 때문이었고 — **ElevenAPI 탭이 별도로 존재** — 그 탭 확인만 남았습니다.

- ✏️ `docs/REQUIREMENTS.md` 7.2 — ElevenLabs도 **상업 라이선스는 Starter($6/월)부터, Free는 비상업.** Tripo와 같은 티어 구조라 BYOK 고지가 동일 적용
- ✏️ `docs/REQUIREMENTS.md` 9절 — 환산율 확보: 효과음 1초 ≈ $0.004~0.008, **우리 크레딧으로 초당 1크레딧 미만.** 캐릭터 원가 추정을 75~85크레딧으로 갱신 — **비용의 실체는 메시·리깅이고 오디오는 오차 범위**라는 것이 이번 확인의 소득
- ✏️ `docs/ARCHITECTURE.md` 13절 2번 — 부분 확인으로 전환, 남은 것을 ElevenAPI 탭 요금·약관으로 좁힘

---

## 2026-08-10 (4차 — Tripo API 약관·단가 확인 반영)

사용자가 Tripo 약관(2025-07-11판)과 API 가격 페이지를 조사(GPT 보조, 원문 링크 확보)해 넘겨줬습니다. 3차의 남은 물음이 대부분 닫혔고, 대신 실행 항목이 하나 생겼습니다.

- ✏️ `docs/REQUIREMENTS.md` 7.2 — **5.2.2: Paid User Output에 광범위한 상업권 + 학습 미사용 명시** (권리 기반 확보). **3.2: end user 대상 생성 서비스 제공에는 사전 서면 승인 필요** — 데모 B에 정면으로 걸리는 조항. 같은 구조의 공식 통합 사례(MakerWorld·Layer AI·Nilo·OpenArt)가 있어 금지가 아니라 파트너 통제로 해석
- ✏️ `docs/REQUIREMENTS.md` 10절 — 승인 거절·지연 위험 추가. 대응은 TRELLIS 전환이며 어댑터 설계(N-8, B-5)가 실행 수단
- ✏️ `docs/REQUIREMENTS.md` 9절 — API 가격 페이지와 재대조. 텍스처 10–30, 리토폴로지 10–30, 리그 체크 무료, 리타깃 10/애니메이션(프리셋 100+) 확보. 캐릭터 1체 원가를 80~90크레딧으로 갱신
- ✏️ `docs/ARCHITECTURE.md` 13절 — 1번을 확인 완료 + 실행 항목(승인 요청)으로 전환. 7번에 긍정 신호 기록 — 가격 페이지가 "post-process **any mesh**"를 명시, 리깅의 외부 메시 지원만 API 문서 확정 남음
- ✏️ `TODO.md` 9단계 — 첫 항목으로 승인 요청 추가. **승인 확보 전 공개 URL을 열지 않음**

⚠️ **조사가 GPT 보조로 이뤄졌으므로 승인 요청 발송 전에 3.2·5.2.2 조항의 원문 문구를 직접 재대조합니다.** 조항 번호나 해석이 어긋나면 7.2를 고칩니다.

---

## 2026-08-10 (3차 — Tripo 권리 부분 확인)

사용자가 Tripo 웹 요금제 화면을 확인해 넘겨줬습니다 — **Free는 공개 모델·비상업, Pro(₩29,700/월) 이상 비공개·상업.** 생성물 권리가 계정 티어에 종속된다는 사실이 확정됐습니다.

- ✏️ `docs/REQUIREMENTS.md` 7.2 — 미확인에서 부분 확인으로. 운영 계정 조건과 BYOK 티어 고지 파급 추가
- ✏️ `docs/ARCHITECTURE.md` 13절 1번 — 남은 확인을 "API 생성물에 같은 원칙이 적용되는지"로 좁힘

⚠️ **웹 구독과 API는 별도 과금입니다.** 이번에 확인된 것은 웹 앱 구독의 권리이고, 서버가 실제로 쓰는 API 경로의 생성물 권리는 여전히 원문 미확인입니다. ElevenLabs도 미확인으로 남아 있습니다.

---

## 2026-08-10 (2차 — 사용 시나리오 검토 반영)

설계 v2를 사용자 시나리오 세 가지(수동 시작→AI 보조 / AI 초안→수동 마무리 / 임포트 후 도중 작업)와 제작 도구형 프론트엔드 직무 요건에 대조해 빈틈을 찾았습니다. 시나리오 ②만 온전히 지원되고 ①·③의 입구가 없었으며, 디자인 시스템이 원칙 한 줄로만 존재했습니다.

- ✏️ `docs/REQUIREMENTS.md` — 작업 입구 3갈래(생성/수동/임포트) 명시, 파이프라인 대상을 "씬 안의 임의 메시"로 재정의, A-11(프리미티브·라이트)·A-12(드래그 앤 드롭) 신설, N-14(디자인 시스템) 신설
- ✏️ `docs/UX.md` — 온보딩에 수동·임포트 입구 병기, 3.7절 드래그 앤 드롭 규칙(전부 커맨드 경유), 6절 디자인 시스템 상세(토큰 3층·두 번들 공유·카탈로그)
- ✏️ `docs/ARCHITECTURE.md` — `client/ui/` 신설과 단방향 의존 규칙, 미확인 목록에 7번(Tripo 외부 메시 지원) 추가
- ✏️ `docs/DECISIONS.md` — D-19(입구 3갈래, 메시 모델링은 계속 범위 밖)·D-20(디자인 시스템 승격)
- ✏️ `TODO.md` — 1단계에 A-11·토큰 골격, 4단계에 입구 재정의 검증, 9단계에 A-12·카탈로그

**협업 P2→P1 상향은 검토 후 접었습니다** — 데모 B가 무거워집니다. 사용자 결정이며 D-19에 남겼습니다.

⚠️ **Tripo가 자사 생성물이 아닌 메시의 리깅·텍스처를 받는지 미확인입니다.** 수동·임포트 입구가 파이프라인을 끝까지 탈 수 있는지가 여기 걸립니다. 받지 않으면 해당 입구는 정리·UV까지로 좁히고 제공자 제약으로 문서화합니다.

---

## 2026-08-10 (설계 v2 — 공개 서비스급 확장)

사용자의 방향 전환 — "단순히 포트폴리오가 아니라 완성도 높은 잘 만든 웹 프로젝트로" — 을 받아 설계 전체를 개정했습니다. 도달점(공개 서비스급), 확장 범위(허브·공유 / 협업 lock / VLM 검수자 / 에디터 심화), 비용 정책(사용자별 크레딧 + 전역 캡)을 대화로 확정한 뒤 진행했습니다.

- ✏️ `docs/REQUIREMENTS.md` — v2 개정. 데모 B(서비스 증명) 신설, 우선순위 의미 재정의(P0 데모 A / P1 데모 B / P2 확장), I절(계정·크레딧)·J절(허브·공유)·K절(협업)·L절(검수자) 신설, 비기능에 보안·백업·개인정보·관측성·브라우저 매트릭스 추가. **C절 게이트 수치와 라이선스 판정 등 검증된 내용은 손대지 않고 보존**
- ➕ `docs/ARCHITECTURE.md` — 구조 정본 신설. 데이터 모델 / API 계약 / 잡 상태 머신 / 크레딧 흐름 / 동기화·lock 프로토콜 / 배포 / 보안 / 확장 경로
- ➕ `docs/UX.md` — 화면 정본 신설. 정보 구조 / 에디터 레이아웃 / 파이프라인 스테퍼 / 핵심 흐름 / 오류 문구 사전
- ✏️ `docs/DECISIONS.md` — D-13~D-18 추가, 뒤집힌 결정 목록 갱신
- ✏️ `docs/ROADMAP.md` — 협업 1단계·VLM 검수자 승격 반영, 결제·수평 확장을 범위 밖으로 추가
- ✏️ `TODO.md` — 9단계(서비스화)·10단계(협업)·11단계(검수자·심화) 신설. 0–8단계는 유지하되 1단계에 커맨드 직렬화 요건, 2단계에 `user_id` 자리 파두기 반영

**설계에서 지킨 축 두 가지.** ① 데모 A는 여전히 서버 없이 로컬만으로 성립합니다 — 서비스 확장이 코어 데모의 완료 조건에 끼어들지 않게 우선순위 구조로 격리했습니다. ② 서버는 정본 저장을 맡게 됐지만 여전히 씬을 해석하지 않습니다 — 리비전은 불투명 블롭입니다.

**커맨드 직렬화 요건을 1단계로 앞당긴 것이 이번 개정의 실질적 비용입니다.** 협업(K-4)을 P2에 두면서도 그 전제 조건만 1단계 완료 판정에 넣었습니다. 나중에 커맨드 스택을 뜯어고치는 것보다 쌉니다.

⚠️ **미확인 항목이 `docs/ARCHITECTURE.md` 13절에 모여 있습니다.** 생성물의 상업적 이용 권리(Tripo·ElevenLabs), ElevenLabs 크레딧 환산율, 스토리지·호스팅 요금이 원문 미확인 상태이며, M9(서비스화) 착수 전에 사용자가 확인해야 합니다. 갤러리 공개가 이 확인에 걸려 있습니다.

---

## 2026-08-09 (2차 — 변경 기록과 의사결정 기록 도입)

같은 워크스페이스의 다른 팀 저장소가 문서를 어떻게 관리하는지 확인한 뒤 두 가지를 들여왔습니다. 위키나 외부 도구 없이 저장소 안 마크다운만으로 굴러가고 있었고, 특히 변경 기록에 판단 근거를 함께 남기는 방식이 이 프로젝트의 평가 기준과 맞았습니다.

- ➕ `CHANGELOG.md` — 이 파일. 세션 단위 작업 기록
- ➕ `docs/DECISIONS.md` — 결정과 접은 대안을 공개 문서로 분리
- ✏️ `TODO.md` — 완료 기록 절을 삭제하고 `CHANGELOG.md`로 위임. 남은 것과 지나간 것을 한 파일에 섞지 않습니다
- ✏️ `CLAUDE.md` — 작업 완료 시 절차에 변경 기록 작성을 추가

**판단 근거를 공개로 옮기되 통째로 옮기지는 않았습니다.** `.local/DECISION_LOG.md`에는 조사 비용과 폐기된 조사 발주, 작업 중 있었던 오독 같은 과정 메타가 섞여 있습니다. 결정과 그 근거만 뽑아 `docs/DECISIONS.md`로 세우고, 나머지는 비공개로 남겼습니다.

⚠️ **공개 문서인 것과 검토자에게 주는 것은 별개입니다.** `docs/DECISIONS.md`는 저장소에 공개되지만 **2차 미지 에이전트 검증에는 넘기지 않습니다.** 판단 근거를 알면 냉정한 검토가 되지 않는다는 조건이 그대로 적용됩니다. `CLAUDE.md`의 2차 조건에 이 파일을 제외 목록으로 명시했습니다.

---

## 2026-08-08 (1차 — 냉정 검토 반영)

맥락을 차단한 에이전트 두 차례의 검토 결과를 반영했습니다. 첫 회차는 문서만 보고 결함을 찾게 했고, 둘째 회차는 판단이 필요했던 네 건에 해결안을 요구했습니다.

**사실 오류 정정** — 전부 이전 조사 결과를 옮겨 적은 뒤 재확인하지 않은 것들이었습니다.

- ✏️ Tripo 리타깃은 텍스트에서 모션을 생성하는 것이 아니라 **고정 프리셋 선택**입니다. 데모의 검 휘두르기는 `slash`로 성립하지만 서술이 거짓이었습니다
- ✏️ `KHR_animation_pointer`가 비준 확장으로 존재합니다. 결론은 유지되나 근거 문장이 틀렸습니다
- ✏️ ElevenLabs는 분당 요금이 아니라 크레딧 단위입니다. 자동 지속시간이면 0.5초짜리도 200크레딧입니다
- ✏️ WebGPU는 Chrome 안드로이드를 지원합니다. 실제 위험은 리눅스 쪽이었습니다
- ✏️ Mixamo 항목이 한 칸 안에서 모순이었습니다. 재배포 금지와 정적 번들 허용이 함께 있었는데, 공개 저장소에서는 번들이 곧 재배포이므로 전면 제외로 통일

Tripo 단가 네 건은 원문과 대조해 모두 정확함을 확인했습니다. **오토리깅 $0.25가 확인되어 후처리 설계를 다시 짤 필요가 없어졌습니다.**

**문서 구조**

- ➕ `docs/WMT_SCHEMA.md` — 익스포터와 Unity 임포터가 공유하는 계약을 요구사항에서 분리
- ✏️ 프레임과 초가 섞여 있던 것을 초를 정본으로 확정. Unity가 재샘플링하므로 프레임을 정본으로 두면 어긋납니다
- ➕ H절(저장과 서버) 신설. 인증과 메타, 스토리지에 요구사항 ID가 없어 **2차 검증이 원리적으로 불가능한 구간**이었습니다
- ✏️ A-6의 저장 매체를 IndexedDB로 확정해 에디터가 서버에 의존하지 않게 함

**판정 기준 수치화** — "자연스럽게", "수만 원" 같은 표현으로는 끝났는지 판정할 수 없었습니다.

- ✏️ N-2를 95백분위 프레임 시간 16.7밀리초로. 평균 60fps는 스터터를 숨깁니다
- ✏️ N-6을 월 $40 하드 캡으로. 80퍼센트 경고, 100퍼센트 거부
- ➕ C-1에 완료 게이트 5종, C-3에 성공 판정 4종

**검증 체계**

- ➕ 0차 데모 레일. 완료 조건이 하나뿐인데 8단계 마지막에서만 확인하고 있었습니다
- ✏️ 2차의 발동 조건을 자기 판정에서 경로 목록으로 변경
- ✏️ 오디오 동기 판정을 3차에서 1차로. 측정 대상도 하드웨어 지연이 아니라 스케줄러가 계산한 예약 시각으로
- ✏️ 라이선스 CI를 금지 목록에서 허용 목록으로 전환

⚠️ **`CLAUDE.md`에 적어둔 "표준 확장을 찾아 헤매지 않는다"가 오류를 얼릴 뻔했습니다.** 시간을 아끼려는 지침이 재검토를 막는 형태였습니다. "판정이 틀렸다는 근거가 나오면 고친다"를 덧붙였습니다.

⚠️ **검증 체계에 스스로 구멍을 냈던 것도 드러났습니다.** 2차의 발동 조건을 "핵심 구간"이라는 판단 여지가 있는 말로 적어두고, 실제로 작업지시서에 "2차 생략"이라고 써놨습니다. 생략은 사용자만 결정하도록 바꿨습니다.

---

## 2026-08-05 (2차 — 작업 진행 체계)

- ➕ `TODO.md` — 전체 작업 목록과 진행 상태
- ➕ `WORK_ORDER.md` — 지금 할 작업 하나의 실행 계획
- ✏️ `CLAUDE.md` — 세션 시작 시 작업지시서를 읽고 완료 시 목록을 갱신하는 규칙

작업지시서를 목록의 발췌가 아니라 **항목 하나의 확대**로 잡았습니다. 목록을 뽑아 옮기면 같은 정보가 두 파일에 존재하게 됩니다. 작업지시서에는 `TODO.md`에 없는 것만 담습니다 — 하는 것과 하지 않는 것의 경계, 완료 판정, 참조할 절, 제약입니다.

완료 시 작업지시서를 비우지 않고 곧바로 다음 것으로 덮어씁니다. 빈 파일을 거치면 그 사이에 세션이 끊겼을 때 다음이 무엇인지를 다시 판단해야 합니다.

---

## 2026-08-05 (1차 — 저장소 초기화)

- ➕ `.gitignore` — `.local/`을 추적 제외로. 판단 기록과 작업 메모가 들어갑니다
- ➕ `.gitattributes` — 줄바꿈과 파일 유형 규칙을 저장소에 고정

`.gitattributes`를 코드가 들어오기 전에 넣었습니다. 나중에 추가하면 이미 들어간 파일을 정규화하는 커밋이 따로 필요하고 그 커밋은 diff가 전부 뒤집힌 채로 남습니다. 실제로 `git add --renormalize`를 돌려보니 기존 파일이 하나도 재작성되지 않아 정규화 커밋 없이 넘어갔습니다.

3D 에셋과 오디오를 바이너리로 지정한 것이 이 프로젝트에서는 실질적입니다. 텍스트 변환이 걸리면 파일이 손상됩니다. `mvnw`는 LF, `mvnw.cmd`는 CRLF로 서로 반대 규칙을 받아야 하는데 그 부분도 확인했습니다.

---

## 2026-08-04 (요구사항 정의)

- ➕ `docs/REQUIREMENTS.md` — 목적, 기능 요구사항, 데이터 설계, 기술 스택, 위험, 완료 조건
- ➕ `docs/ROADMAP.md` — 1차 범위 밖 설계. 협업 lock과 VLM 검수자
- ➕ `CLAUDE.md` — 작업 지침과 제약

목표를 포트폴리오와 기술 증명으로 확정하고, 그에 따라 1차 구현 범위에서 협업을 제외했습니다. 근거는 `docs/DECISIONS.md`에 있습니다.

⚠️ **조사를 발주하기 전에 목표를 먼저 확정했어야 했습니다.** 시장과 경쟁사를 포함한 조사를 먼저 던졌다가, 목표가 포트폴리오로 정해지면서 그 절반이 불필요해져 폐기했습니다. 목표가 조사 범위의 절반을 정합니다.

⚠️ **조사를 병렬로 쪼개면 각 에이전트가 서로의 결과를 모릅니다.** 한쪽이 "자동 리깅이 데모를 무너뜨린다"며 리깅 제외를 권고했는데, 다른 쪽이 찾은 Tripo 오토리깅 $0.25가 그 전제를 뒤집었습니다. 회수 후 교차 검토를 반드시 거칩니다.
