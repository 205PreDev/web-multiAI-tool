# CLAUDE.md — web-multiAI-tool

이 파일은 Claude Code가 이 프로젝트에서 작업할 때의 지침이다.

## 프로젝트

**웹 브라우저에서, 텍스트와 이미지로부터 게임 엔진에 바로 넣을 수 있는 3D 에셋을 만드는 멀티모달 AI 에디터.**
메시·텍스처·리깅·애니메이션·효과음을 서로 다른 도구로 나눠 만들지 않고 하나의 타임라인 위에서 함께 만든다.

- 목표는 **포트폴리오 / 기술 증명**. 상용 서비스가 아니다. 평가 기준은 ① 완성도 ② 설명 가능성.
- 상태: **요구사항 정의 완료, 구현 미착수.**

## 문서

| 파일 | 내용 | 언제 읽는가 |
|---|---|---|
| `docs/REQUIREMENTS.md` | **요구사항 정본.** 목적 / 기능 요구사항 A–G / 데이터 스키마 / 스택 / 위험 | 작업 전 항상 |
| `docs/ROADMAP.md` | 1차 범위 밖 설계 — 협업 lock, VLM 검수자 등 | 범위 밖 항목을 물을 때 |
| `docs/web-multiAI-tool.md` | 개요 노트 | 전체를 빠르게 훑을 때 |
| `.local/DECISION_LOG.md` | 판단의 경위, 폐기된 대안, 조사 회수 결과 | "왜 그렇게 정했나"를 물을 때. **추적 제외** |
| `TODO.md` | 전체 작업 목록과 진행 상태 | 진행 상황을 묻거나 갱신할 때 |
| `WORK_ORDER.md` | 지금 할 작업 하나의 실행 계획 | **세션 시작 시 항상** |

**`docs/`는 옵시디언 볼트 `G:\이영호\web-dev\web-multiAI-tool`로의 디렉터리 정션이다.** 파일의 실체는 볼트에만 있고 `docs/`는 그것을 비추는 창일 뿐이다. 어느 쪽 경로로 읽고 써도 같은 파일이므로 사본이 생기지 않는다.

**git은 정션을 평범한 폴더로 취급해 내용물까지 들어간다**(실측 확인). 따라서 `docs/`의 문서는 코드와 같은 저장소에 일반 파일로 커밋되고, 클론한 사람은 정션이 아니라 실제 파일을 받는다.

> `git clean -xdf`는 추적되지 않는 파일을 강제로 지우므로 **볼트 원본에 닿을 수 있다.** `docs/`에 대해 이 명령을 쓰지 않는다. 단, 브랜치 전환으로는 볼트 원본이 지워지지 않는 것을 확인했다.

문서 본문에 이 배치를 적지 않는다 — 문서는 내용만 담는다.

## 작업 진행 규칙

1. 세션을 시작하면 **`WORK_ORDER.md`를 먼저 읽는다.** 지금 할 작업 하나가 거기에 있다.
2. **진행 상태의 정본은 `TODO.md`다.** 상태를 다른 곳에 중복해 적지 않는다.
3. 작업을 끝내면 순서대로 처리한다.
   ① `TODO.md`의 해당 항목을 `[x]`로 바꾸고 완료 기록에 날짜와 한 줄 요약을 남긴다.
   ② `TODO.md`에서 다음 미착수 항목을 골라 `WORK_ORDER.md`를 **새로 쓴다.** 비워두지 않는다.
   ③ 사용자에게 무엇이 끝났고 다음이 무엇인지 한 줄로 보고한다.
4. `WORK_ORDER.md`는 **항상 작업 하나만** 담는다. 목록을 옮겨 적지 않는다.
5. 요구사항이 바뀌면 `docs/REQUIREMENTS.md`를 먼저 고치고 `TODO.md`를 맞춘다. 순서를 뒤집지 않는다.
6. 지시서의 절차가 현장과 맞지 않으면 그대로 따르지 말고 **먼저 알린다.** 지시서는 판단을 대신하지 않는다.

## 검증 3단계

작업이 끝나면 아래 순서로 검증한다. **앞 단계를 통과해야 다음으로 넘어간다.**

| 단계 | 하는 일 | 언제 |
|---|---|---|
| **1차 — 도구 검증** | 빌드 / 타입 / 린트 / 테스트를 통과시킨 뒤 `/code-review` | 작업지시서 1건마다 |
| **2차 — 미지 에이전트 검증** | 이 세션의 맥락을 모르는 에이전트가 요구사항과 대조해 결함을 찾는다 | 아래 발동 조건 참조 |
| **3차 — 사용자 검증** | 보고 듣고 만져야 아는 것 | 병합 전 항상 |

기계 검증이 먼저다. 빌드가 깨진 코드에 리뷰를 돌리면 지적이 엉뚱한 곳을 향한다.

### 0차 — 데모 레일

**완료 조건이 하나뿐인 프로젝트이므로 그 하나를 매번 확인한다.** 대표 데모의 각 단계 산출물을 픽스처로 저장소에 두고, 외부 API를 호출하지 않은 채 그 파일부터 아래로 흐르는 경로를 통째로 재검사한다.

```
fixtures/demo/
  01_generated.glb    3단계 완료 시 커밋 — Tripo 텍스트→메시의 실제 응답
  02_cleaned.glb      4단계 — C-1 게이트 통과본
  03_rigged.glb       4단계 — C-3 판정 통과본
  04_animated.glb     5단계 — slash 클립과 hit 이벤트 포함
  05_sfx_slash.mp3    6단계
  06_final.glb        8단계 — wmt extras 포함 최종본
  expected.json       각 단계의 판정 수치
```

- `npm run demo:rail` 한 명령으로 돈다. **외부 API를 부르지 않으므로 비용이 0이고 매 PR에서 실행한다.**
- 아직 없는 픽스처의 단계는 건너뛴다. 3단계 완료 시점부터 실질적으로 동작한다.
- **각 단계의 마지막 TODO 항목은 "데모 레일에 이 단계의 픽스처를 추가하고 통과시킨다"로 고정한다.**
- 픽스처를 바꾸는 PR은 왜 바꿨는지를 본문에 적는다. **픽스처 교체로 테스트를 통과시키는 것은 회귀를 지우는 것과 같다.**
- 1차 도구 검증에 `npm run demo:rail` 통과를 포함한다.
- **C-2 서버 폴백은 대표 데모에서 발동하지 않으므로 삼각형 15만짜리 픽스처를 따로 두어 강제로 태운다.** 실행되지 않는 경로는 어느 런타임에 있든 죽어 있다.

### 2차의 조건

**에이전트에게 판단 근거를 주지 않는다.** 변경된 코드와 `docs/REQUIREMENTS.md`의 해당 절, `CLAUDE.md`의 제약, 완료 판정 기준만 넘긴다. `WORK_ORDER.md`의 근거와 `.local/`은 넘기지 않는다. **`git log`와 PR 본문도 넘기지 않는다** — 저장소 규칙이 PR에 근거를 적게 하므로 이력에는 차단하려던 것이 그대로 남아 있다. 읽기 도구만 가진 에이전트로 띄운다.

**발동 조건 — 수행자가 판단하지 않는다.** 아래 중 하나에 걸리면 반드시 실행한다.

1. `TODO.md`의 한 단계를 끝냈을 때. **0단계는 예외** — 스캐폴딩에는 대조할 구현 로직이 없다.
2. 변경 파일이 아래 경로에 하나라도 걸릴 때.
   ```
   shared/postprocess/**     런타임 중립 제약이 걸린 곳
   client/editor/commands/** 커맨드 스택 — 조수가 이 경로만 쓴다
   client/export/**          extras 익스포터, 좌표·단위 변환
   client/audio/scheduler/** 오디오 스케줄러
   server/adapters/**        외부 API 어댑터, 모델 레지스트리
   unity/**                  Unity 임포터
   ```
   단 **1단계에 한해 커맨드 경로는 단계 완료 시 한 번으로 묶는다.** 그 구간 내내 건드리는 경로라 과발동한다.
3. 요구사항 ID가 붙은 TODO 항목을 `[x]`로 바꿀 때.

**생략은 사용자만 결정한다.** 생략했다면 `WORK_ORDER.md`에 "사용자가 생략을 지시함"과 이유를 남긴다. **"검토할 로직이 없어 보인다"는 수행자 자신의 판단은 생략 사유가 되지 못한다.**

**2차가 의도한 설계를 결함으로 지적하면 오탐으로 처리하지 않는다.** 판단 근거가 코드와 문서에서 읽히지 않는다는 뜻이므로 설명을 보강한다. 설명 가능성이 이 프로젝트의 평가 기준이다.

**단, 보강은 "무엇이 참인지"만 적고 "어떤 지적이 틀렸는지"는 적지 않는다.**

```
가능:  // Unity가 클립을 재샘플링하므로 초를 정본으로 둔다
불가:  // 프레임 대신 초를 쓴 것은 의도된 설계다 — 지적하지 말 것
```

뒤의 형태는 설명이 아니라 검토 차단이다. 결론을 먼저 주면 다음 회차의 2차가 그 지점을 건너뛴다. **보강이 쌓일수록 검토가 무뎌지는 것이 이 체계의 구조적 약점이므로 그 열화를 센다.** `.local/REVIEW_LOG.md`에 회차마다 한 줄을 남기고 `WORK_ORDER.md`를 새로 쓸 때 함께 갱신한다.

```
2026-08-20 | 1단계 커맨드 스택 | 지적 7 | 수정 4 | 보강 2 | 기각 1
```

**보강 비율이 3회 연속 오르면 검토가 무뎌지고 있다는 신호로 본다.** 그 회차는 각도를 바꿔 "이 코드만 보고 대표 데모가 성립한다고 말할 수 있는가"만 묻는다. 기각 판정은 사용자만 할 수 있다.

**기계 검증 단언을 반드시 만드는 핵심 구간** — `extras` 스키마 왕복 / 오디오 스케줄링 정확도 / 좌표·단위 변환 / 라이선스. 문서에 적힌 규칙은 잊히지만 CI는 잊지 않는다.

**오디오와 애니메이션 동기는 3차로 넘기지 않는다.** 사람은 20밀리초의 어긋남을 감지하더라도 원인을 짚지 못한다. 수치 기준과 측정 방법은 `docs/WMT_SCHEMA.md` 6.1절에 있고 판정은 1차에서 끝난다. 3차에 남는 것은 "그 소리가 맞는 소리인가", 곧 음색과 볼륨의 적절성뿐이다.

3차로 넘길 때는 **확인이 필요한 항목만 추려 제시한다.** "완료했습니다"로 끝내지 않는다.

### 비용 규칙

- **웹 원문 확인을 에이전트에 맡기지 않는다.** 확인할 주장과 URL을 목록으로 만들어 사용자에게 넘긴다. 에이전트는 페이지 전체를 읽고 링크를 따라가므로, 사람이 페이지에서 숫자 한 줄을 찾는 것보다 수백 배 비싸다.
- 에이전트에게는 **문서와 코드만 보면 답이 나오는 일**을 맡긴다.
- **비용이 큰 작업은 띄우기 전에 예상 규모를 알리고 동의를 받는다.** 사용자가 자리를 비운 사이에 시작하지 않는다.

## 저장소 규칙

- **`main`에 직접 커밋하지 않는다.** 작업지시서 한 건이 브랜치 하나이고 PR 하나다.
- 브랜치 이름은 `wo/<단계>-<요약>` 형식을 쓴다. 예를 들어 `wo/0-scaffold`처럼 적는다.
- 브랜치 푸시와 PR 생성은 검증 수단이므로 자유롭게 한다. **`main` 병합만 사용자 승인을 받는다.**
- PR 본문에는 무엇을 했는지보다 **왜 그렇게 했는지**를 적는다. PR 이력 자체가 산출물이다.

## 완료 조건

> `docs/REQUIREMENTS.md` 1.4절의 대표 데모 — 텍스트로 캐릭터 생성 → 자동 리깅 → 모션 적용 → 타격 프레임에 생성한 SFX 바인딩 → Unity 익스포트 후 재생 — 을 브라우저만으로 끊김 없이 시연.

P0 항목이 이 데모를 성립시킨다. P1·P2는 잘라도 데모가 무너지지 않는다.

## 기술 스택 (예정)

```
클라이언트     React 19 + TypeScript + Vite
              @react-three/fiber + drei + leva
              three.js WebGPURenderer (WebGL2 자동 폴백)
              Zustand (씬 그래프 + 커맨드 스택), Web Audio API
브라우저 WASM  glTF-Transform + meshoptimizer + Draco   ← 서버와 코드 공유
              xatlas-three (웹워커) — 실패 시 서버 폴백
서버          Spring Boot (인증 · 메타 · 작업 큐 · WebSocket/STOMP)
외부 API      Tripo · fal.ai TRELLIS · ElevenLabs SFX · Claude(tool calling)
```

기존 `C:/dep/3DCommunity`(React + three.js + Spring Boot STOMP) 자산을 그대로 재사용하는 구성이다.

---

## 반드시 지킬 제약

### 1. 도입 금지 (라이선스)

| 대상 | 사유 |
|---|---|
| **Hunyuan3D 2.x 전 계열** | 라이선스 "Territory" 정의가 **대한민국을 명시적으로 제외**. 상업·비상업 무관, 호스팅 경유도 근거 없음 |
| AudioGen / AudioLDM2 | 가중치가 CC-BY-NC 계열 |
| MoMask / MDM 등 모션 OSS | SMPL·AMASS·HumanML3D 데이터셋 학술 제한 전이 |
| RigNet | 비상업 라이선스 |
| Mixamo 런타임 연동 | 공개 API 없음. 모션 파일 재배포는 약관 위반 |

새 모델·데이터셋을 제안할 때는 **코드와 가중치 라이선스를 각각 확인**한다. 코드가 MIT여도 가중치가 비상업인 사례가 흔하다.

#### CI 검사 — 금지 목록 매칭만으로는 못 잡는다

이름 매칭은 두 경우를 놓친다. 금지 모델을 호스팅 서비스의 엔드포인트 URL이나 별칭 뒤에 두었을 때, 그리고 코드가 MIT이고 가중치가 비상업일 때다. **7.1절이 경고한 바로 그 유형이므로 검사를 뒤집는다.**

**모든 외부 생성 모델 호출은 `server/adapters/registry.ts`에 등록된 항목을 통해서만 나간다.** 등록 항목은 아래를 전부 채운다.

```ts
{
  id: 'tripo-text-to-3d',
  provider: 'tripo',
  endpoint: 'https://api.tripo3d.ai/...',
  codeLicense: 'proprietary-saas',
  weightsLicense: 'hosted-service',   // 우리가 가중치를 배포하지 않음
  verifiedAt: '2026-08-08',
  verifiedUrl: '...',
}
```

CI가 셋을 검사한다.

1. **레지스트리 항목의 필드가 하나라도 비면 실패한다.** `codeLicense`와 `weightsLicense`를 별도 필드로 둔 것이 "코드가 MIT이니 괜찮다"를 막는 장치다.
2. **`weightsLicense`가 `MIT` / `Apache-2.0` / `BSD-3-Clause` / `hosted-service` 이외면 실패한다.** 새 값을 허용하려면 목록 자체를 고쳐야 하고 그 변경은 PR에 남는다.
3. **`server/adapters/` 밖에서 외부 호스트로 나가는 리터럴 URL이 발견되면 실패한다.** 레지스트리 우회 경로를 막는다. `client/experiments/`만 예외로 두고 그 폴더는 프로덕션 번들에서 배제한다.

보조로 저장소 전체 텍스트에서 금지 키워드를 찾는다 — `hunyuan` / `audiogen` / `audioldm` / `momask` / `rignet` / `mixamo` / `smpl` / `amass` / `humanml3d`(대소문자 무시). **`docs/REQUIREMENTS.md` 7.1절과 이 파일은 검사에서 제외한다** — 금지 목록 자체가 그 단어들을 담고 있어 제외하지 않으면 CI가 항상 실패한다.

빌드 산출물에 제공자 키 문자열이 없음을 단언하는 테스트도 함께 둔다. `.env`가 추적 제외인 것만으로는 번들 유입과 오류 응답 유출을 막지 못한다.

### 2. 후처리 순서를 바꾸지 않는다

```
정리(glTF-Transform) → UV(xatlas) → 리깅(Tripo API) → LOD
```

AI 생성 메시의 토폴로지가 나쁘면 리깅 품질이 함께 무너진다. 리깅 전에 정리·UV를 반드시 통과시킨다.

### 3. glTF의 한계를 전제로 설계한다

- **glTF에는 오디오도 애니메이션 이벤트도 없다.** Khronos 비준 확장에 오디오 부재, `KHR_audio_graph`는 Proposal, `MSFT_audio_emitter`는 three.js 미지원.
- 기본 애니메이션 채널은 translation/rotation/scale/weights뿐이다. `KHR_animation_pointer`(비준)가 임의 속성까지 넓히지만, **그것은 "무엇을 움직이는가"이지 "특정 시점에 이름 붙은 사건"이 아니다.** 이벤트 개념은 명세 어디에도 없다.
- → **`extras`의 `wmt` 네임스페이스 커스텀 스키마가 유일한 경로.** 계약은 `docs/WMT_SCHEMA.md`가 정본이고, 설계 근거는 `docs/REQUIREMENTS.md` 6.2절에 있다.
- 표준 확장을 다시 뒤지는 데 시간을 쓰지 않는다. 다만 **위 판정이 틀렸다는 근거가 나오면 이 항목을 고친다.** 결론을 근거보다 오래 살려두지 않는다.
- 담을 수 없는 것: IK, 본 컨스트레인트, 머티리얼 노드 그래프, 물리/콜라이더, 스테이트 머신, 커스텀 셰이더. 스키닝은 정점당 4본, 모프 타깃은 런타임 상위 8개.

### 4. 좌표·단위 변환

glTF는 미터 / +Y up / **−Z forward**. Unity는 +Y up / **+Z forward**. FBX는 센티미터.
Unity에 glTF 기본 임포터가 없다 — **glTFast 또는 UnityGLTF가 전제**다.
Unreal(+Z up / 좌수)은 범위 밖. 문의 시 "FBX 경로 권장"으로 답한다.

### 5. 오디오 타이밍

`requestAnimationFrame`에 맞춰 소리를 트리거하지 않는다. **`AudioContext.currentTime` 기반 룩어헤드 스케줄러**(25ms 틱 / 100ms 선행)로 예약하고 렌더 클럭과 분리한다.

### 6. LLM 조수 (tool calling)

- 씬 상태는 **요약 JSON + 개별 상세 질의 + 뷰포트 스크린샷** 3분할로 제공한다. 전체 씬을 통째로 넣지 않는다. (참고: `ahujasid/blender-mcp`)
- 프롬프트 캐싱은 `tools → system → messages` 접두사 일치를 본다. **가변 씬 상태는 마지막 캐시 분기점 이후, messages 끝**에 둔다. 툴 목록 순서는 고정한다.
- 툴 스키마는 `strict: true` + `additionalProperties: false`. 각 툴 설명에 **언제 호출하는지**를 쓴다.
- **조수는 새 조작 경로를 만들지 않는다. 이미 있는 에디터 커맨드만 호출한다.** 툴 하나가 커맨드 하나에 1:1로 대응하면 undo는 자동으로 따라온다. 조수가 자기만의 씬 변경 경로를 가지면 커맨드 스택 설계가 오염된다.
- 툴은 두세 개로 한정한다. 트랜스폼 변경과 머티리얼 변경이면 역할이 증명된다.
- 프롬프트 캐싱에는 모델별 최소 접두사 길이가 있어 짧으면 조용히 캐시되지 않는다. 배치 규칙은 지키되 캐시 효과를 전제로 삼지 않는다.

### 7. 시크릿

모든 API 키는 서버 보관. **클라이언트 번들에 포함 금지.** `.env`는 추적 제외되어 있다.

### 8. 에디터 기본기는 차용한다

undo/redo · 씬 직렬화 · 다중 선택 · 카메라 조작은 데모에 보이지 않으면서 개발 기간의 상당분을 소모한다(이 프로젝트 최대 위험).
**three.js 공식 에디터(MIT)의 Command 패턴을 차용한다.** 직접 설계하지 않는다.

---

## 실행 (스캐폴딩 후 갱신)

```bash
# 미정 — 프로젝트 초기화 시 작성
```
